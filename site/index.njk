---
layout: default
title: Home
---

{% set dice = dice %}
{% import "components/_chooseDiceButton.njk" as chooseDiceButton %}


<main class="container flex flex-col justify-center flex-1 max-w-2xl space-y-8 sm:py-8 sm:space-y-16">


  <h1 class="text-3xl leading-none text-center text-red-500 lowercase sm:text-5xl">&ldquo;DnD Dice Roller&rdquo;</h1>

  <div class="flex flex-col w-full max-w-sm px-2 mx-auto space-y-8 sm:px-0"
       x-data="{
        amount: '20',
        outcome: '​',
        clearValue: true,
        timeoutAmount: 0
       }">

    {% include "components/_resultDisplay.njk" %}


    <div class="flex-1 justify-stretch">

      <div class="grid flex-1 grid-cols-3 row-gap-4 col-gap-5">

        {% for die in dice %}

        {{ chooseDiceButton.rollDice(die.amount, loop.index, die.selected)}}

        {% endfor %}
        {% include "components/_rollButton.njk" %}
      </div>

    </div>

  </div>
</main>

<footer class="fixed bottom-0 left-0 right-0 p-3 sm:p-4">
  <p class="text-sm text-center text-red-500">made by <a class="underline" href="https://stephenbarkan.com"
       target="_blank">Stephen Barkan</a></p>
</footer>