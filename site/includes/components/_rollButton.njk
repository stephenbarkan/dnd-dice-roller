      <button
              class="px-8 py-4 text-2xl tracking-wide text-gray-700 text-gray-800 transition duration-150 ease-in-out bg-white border border-gray-400 rounded-lg shadow-button hover:border-gray-400 hover:bg-gray-100"
              style="--shadowColor: 74, 85, 104"
              @click="
              if (clearValue === true) {
                timeoutAmount = 1
              } else {
                timeoutAmount = 150
                clearValue = true
              }

              setTimeout( function() {
                clearValue = false
                outcome = Math.ceil(Math.random() * amount)
              }, timeoutAmount)">

        Roll a d<span x-text="amount"></span>
      </button>