      <div class="relative pb-full">
        <a href="#" id="rollDice"
           class="absolute inset-0 flex cursor-pointer rounded-xl"
           @click.prevent
           @click="
              if (clearValue === true) {
                timeoutAmount = 1
              } else {
                timeoutAmount = 150
                clearValue = true
              }

              setTimeout( function() {
                clearValue = false
                outcome = Math.ceil(Math.random() * amount)
              }, timeoutAmount)">

          <span
                class="relative flex flex-col items-center justify-center flex-1 pb-2 mt-2 text-2xl leading-none tracking-wide text-center transition-all duration-200 ease-smooth btnSide rounded-xl">

            <span
                  class="absolute flex items-center justify-center w-full transition-all duration-200 ease-smooth btnTop rounded-xl">
              roll!
            </span>
          </span>
        </a>
      </div>