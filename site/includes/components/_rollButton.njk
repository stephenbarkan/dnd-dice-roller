      <button id="rollButton"
              class="text-2xl tracking-wide transition-all duration-150 ease-in-out border rounded-lg shadow-roll text-roll-text bg-roll-bg border-roll-border active:text-roll-active-text active:bg-roll-active-bg active:border-roll-active-border active:shadow-roll-active hover:bg-roll-hover-bg"
              style="--shadowColor: 100,100,100"
              @click="
              if (clearValue === true) {
                timeoutAmount = 1
              } else {
                timeoutAmount = 150
                clearValue = true
              }

              setTimeout( function() {
                clearValue = false
                outcome = Math.ceil(Math.random() * amount)
              }, timeoutAmount)">
        <span class="relative block transition-transform duration-150 ease-in-out">roll!</span>
      </button>