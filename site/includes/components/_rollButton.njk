      <button
              class="px-8 py-4 text-2xl tracking-wide text-red-600 transition duration-150 ease-in-out bg-red-400 border border-red-500 border-opacity-50 rounded-lg shadow hover:border-opacity-50 hover:bg-red-600"
              style="--shadowColor: 100,100,100"
              @click="
              if (clearValue === true) {
                timeoutAmount = 1
              } else {
                timeoutAmount = 150
                clearValue = true
              }

              setTimeout( function() {
                clearValue = false
                outcome = Math.ceil(Math.random() * amount)
              }, timeoutAmount)">

        Roll a d<span x-text="amount"></span>
      </button>